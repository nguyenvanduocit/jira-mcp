version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: jira-mcp
    main: .
    binary: jira-mcp
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

  - id: jira-cli
    main: ./cmd/jira-cli
    binary: jira-cli
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: jira-mcp
    ids:
      - jira-mcp
    name_template: "jira-mcp_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

  - id: jira-cli
    ids:
      - jira-cli
    name_template: "jira-cli_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "checksums.txt"

brews:
  - name: jira-mcp
    ids:
      - jira-mcp
    repository:
      owner: nguyenvanduocit
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/nguyenvanduocit/jira-mcp"
    description: "jira-mcp - MCP server"
    install: |
      bin.install "jira-mcp"

  - name: jira-cli
    ids:
      - jira-cli
    repository:
      owner: nguyenvanduocit
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/nguyenvanduocit/jira-mcp"
    description: "jira-cli - CLI tool"
    install: |
      bin.install "jira-cli"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
